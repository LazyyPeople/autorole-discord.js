{
  "name": "auto-role",
  "main": "dist",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc && tsc-alias",
    "start:dev": "ts-node-dev -r tsconfig-paths/register --transpile-only --respawn ./src/index.ts",
    "start:prod": "yarn run build && node dist/index.js"
  },
  "dependencies": {
    "discord.js": "^13.7.0",
    "fs": "^0.0.1-security",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.0",
    "ts-node-dev": "^2.0.0",
    "tsc-alias": "^1.6.7",
    "tsconfig-paths": "^4.0.0",
    "typescript": "^4.7.2"
  }
}
